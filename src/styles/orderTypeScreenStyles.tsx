import { StyleSheet } from "react-native";

export const orderTypeScreenStyles = StyleSheet.create({
  mainWrapper: {
    flex: 1,
    flexDirection: "column",
    backgroundColor: "white",
    justifyContent: "space-between",
    alignItems: "center",
    paddingVertical: 150,
  },
});

import { StyleSheet } from "react-native";

export const renderBtnStyles = StyleSheet.create({
  button: {
    flex: 1,
    justifyContent: "flex-end",
    paddingBottom: 20,
  },
  wrapper: {
    flex: 1,
    backgroundColor: "white",
  },
});
